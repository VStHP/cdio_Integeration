<div id="info_club_<%= task.id %>" class="sortable content-section">
  <div class="profile_top_view">
    <div class="row">
      <div class="col-md-12">
        <h4 class="view-control task-tlt"><%= task.name %></h4>
      </div>
      <!--
      <div class="col-md-2">
        <% unless current_user.trainee? %>
          <span class="form-btn-edit">
            <%= link_to task, method: :delete, data: {confirm: "Are you sure you want to delete this task?"},
              class: "btn btn-sm btn-danger", remote: true do %>
              <i class="fa fa-trash-o"></i>
            <% end %>
            <%= link_to edit_task_path(task), class: "btn btn-sm btn-warning", remote: true do %>
              <i class="fa fa-pencil"></i>
            <% end %>
          </span>
        <% end %>
      </div>-->
    </div>
  </div>
  <table class="table table_profile">
    <tr>
      <td class="view-field">
        <strong class="view-control">Youtube's Video</strong>
      </td>
      <td class="profile_content">
        <% if task.link_video.present? %>
          <a href="https://www.youtube.com/watch?v=<%= task.link_video %>" target="_blank">
            https://www.youtube.com/watch?v=<%= task.link_video %>
          </a>
        <% end %>
      </td>
    </tr>
    <tr>
      <td class="view-field">
        <strong class="view-control">Description</strong>
      </td>
      <td class="profile_content"><%= task.description %></td>
    </tr>
  </table>
</div>
