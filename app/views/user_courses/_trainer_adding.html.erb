<% size = @trainer_courses.size %>
<% kt = false %>
<% if @count % @quality == 0 %>
  <td style="padding: 2px; border-right: 1px solid;">
<% end %>
    <div class="checkbox">
      <% @count += 1 %>
      <input type="checkbox" id="<%= trainer.id %>" name="ids[]" value="<%= trainer.id %>"/>
      <label style ="padding: 0px;" for="<%= trainer.id %>" >
        <span></span><%= trainer.name %>
      </label>
    </div>
<% if kt %>
  </td>
<% end %>
